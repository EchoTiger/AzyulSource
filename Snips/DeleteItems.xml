<CustomBehavior File="RunCode" >
<![CDATA[
	foreach (WoWItem i in Me.BagItems) {
	if(i != null && Contains(Settings.Instance.DeleteItemNames, i.Name))
		i.PickUp();
		Lua.DoString("DeleteCursorItem();");
		//Sleep isn't required.
	}
]]>
</CustomBehavior>